# 🚀 Fullstack Wishlist App (Spring Boot + React)

Este é um projeto desenvolvido para fins de aprendizado, conectando uma API robusta em **Java (Spring Boot)** com um front-end dinâmico em **React**. O objetivo principal é colocar em prática conceitos de integração, consumo de APIs REST e persistência de dados.

---

## 🎯 Objetivo do Projeto
O projeto consiste em uma aplicação de **Lista de Desejos (Wishlist)**, onde o usuário pode cadastrar itens que deseja adquirir, visualizar a lista em tempo real e gerenciar essas informações através de uma interface moderna.

**O que eu estou praticando aqui:**
* **Back-end:** Criação de endpoints REST, injeção de dependência e conexão com banco de dados (H2/PostgreSQL).
* **Front-end:** Hooks (`useState`, `useEffect`), chamadas assíncronas com `fetch` ou `Axios` e componentização.
* **Integração:** Resolução de erros de CORS e padronização de JSON entre as camadas.

---

## 🛠️ Tecnologias Utilizadas

### Back-end
* **Java 17+**
* **Spring Boot 3**
* **Spring Data JPA** (Persistência)
* **H2 Database** (Banco em memória para desenvolvimento)
* **Maven** (Gerenciador de dependências)

### Front-end
* **React** (via Vite)
* **JavaScript / JSX**
* **CSS Modules** 

---

## 📁 Estrutura do Repositório

/backend    -> Código fonte da API Spring Boot
/frontend   -> Código fonte da aplicação React
